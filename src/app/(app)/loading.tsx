import { BrandedLoader } from "@/components/BrandedLoader";

/** Branded loading state for authenticated pages (inside Nav/Footer shell). */
export default function AppLoading() {
  return <BrandedLoader />;
}
